name: Staging deployment

on:
  push:
    branches: [ master ]

jobs:
  staging_prepare:
    runs-on: self-hosted
    steps:
      - name: Staging prepare 🚗
        run: |
            echo "Staging prepare."
